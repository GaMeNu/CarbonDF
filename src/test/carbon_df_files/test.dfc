fun ret_test(input: num, input2: str) -> (res: num, res2: str){
    line wawa = mult(input, input2);
    return(wawa, "1", 12);
}

fun main(){
    line val = 1;
    _, _ = ret_test(val, val);
}

/**
 * SetVariable::assign(res, test) // res = test;
 * SetVariable::assign(res2, "Hello world!")
 * Control::return();
 */