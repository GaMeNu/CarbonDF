fun testing(in1: num, in2*?: num) -> (out: num){

    line out2: dict = {
        "res": in1,
        "res2": game.player_count
    };
}

hidden fun main(){
    line x = mult(1, 2);
    x = testing(x, x, x);
    sendMessageSeq("a", "b", 1);
}