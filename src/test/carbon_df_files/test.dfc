
fun multiply(n: num, m: num) -> (result: num){
    line res = mult(n, m);
    return(res);
}

extern fun multi_return() -> (num1: num, num2: num);

fun main() {

    // Return variables and multiple returns
    line aa = multiply(1, 2);
    line
        num1: num,
        num2: num
    = multi_return();

    // Complex items
    _ = {
        "test": aa,
        "test2": 10,
        "dicting": {
            "hello": "world"
        },
        "listing": [
            1,
            [ 2 ],
            3
        ]
    };

}